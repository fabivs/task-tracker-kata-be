services:
  backend:
    build: .
    ports:
      - "8080:8080"
    environment:
      - KTOR_ENV=production

  frontend:
    build: ../task-tracker-kata-fe
    ports:
      - "5173:80"
    depends_on:
      - backend
